document.addEventListener("DOMContentLoaded", function () {
  // SVG code for India map with flag colors
  const indiaMapSVG = `
    <!-- Insert your SVG code here -->
  `;

  // Insert the SVG code into the container
  const indiaMapContainer = document.getElementById("india-map-container");
  indiaMapContainer.innerHTML = indiaMapSVG;
});
